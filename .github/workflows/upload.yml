name: Upload

# 署名とアップロードに必要な Secrets (EXPORT_OPTIONS / APPLE_API_KEY_BASE64 /
# APPLE_API_KEY_ID / APPLE_API_ISSUER_ID) が未設定のため、自動実行は止めて手動実行のみにしている。
# 設定後は main 以外のブランチへの push をトリガーに戻す。
on:
  workflow_dispatch:

concurrency:
  group: upload-${{ github.ref }}
  cancel-in-progress: true

jobs:
  upload:
    name: ${{ github.ref_name }}
    runs-on: macos-latest

    steps:
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Archive
        run: |
          xcodebuild archive \
            -project notti.xcodeproj \
            -scheme notti \
            -sdk iphoneos \
            -configuration Release \
            -archivePath build.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CURRENT_PROJECT_VERSION=${{ github.run_number }}

      - name: Create ExportOptions.plist
        run: echo '${{ secrets.EXPORT_OPTIONS }}' > ExportOptions.plist

      # altool は ./private_keys/AuthKey_<Key ID>.p8 を自動で探すので、ファイル名は Key ID にする
      - name: Create private key
        run: |
          mkdir private_keys
          echo -n '${{ secrets.APPLE_API_KEY_BASE64 }}' | base64 --decode > ./private_keys/AuthKey_${{ secrets.APPLE_API_KEY_ID }}.p8

      - name: Export IPA
        run: |
          xcodebuild -exportArchive \
            -archivePath build.xcarchive \
            -exportOptionsPlist ExportOptions.plist \
            -exportPath app.ipa \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$(pwd)/private_keys/AuthKey_${{ secrets.APPLE_API_KEY_ID }}.p8" \
            -authenticationKeyID ${{ secrets.APPLE_API_KEY_ID }} \
            -authenticationKeyIssuerID ${{ secrets.APPLE_API_ISSUER_ID }}

      # 書き出しと同じ API キーで認証する。Apple ID 認証は Bundle ID からチームを逆引きするため、
      # App Store Connect にアプリが無いと「provider public id を決定できない」で止まる
      - name: Upload IPA to App Store Connect
        run: |
          xcrun altool --upload-app -f app.ipa/notti.ipa --type ios \
            --apiKey ${{ secrets.APPLE_API_KEY_ID }} \
            --apiIssuer ${{ secrets.APPLE_API_ISSUER_ID }}

      - name: Upload IPA to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: notti.ipa
          path: app.ipa/notti.ipa
